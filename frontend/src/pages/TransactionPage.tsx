import TransactionForm from "@/components/TransactionForm";

const TransactionPage = () => {
  return (
    <div className="w-full min-h-screen flex justify-center items-center ">
      <TransactionForm type="Update" />
    </div>
  );
};

export default TransactionPage;
